import React from "react";
import ReactDOM from "react-dom/client";
import DeliveryEntryApp from "./DeliveryEntryApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DeliveryEntryApp />
  </React.StrictMode>
);
