import React from "react";
import ReactDOM from "react-dom/client";
import CollectionEntryApp from "./CollectionEntryApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CollectionEntryApp />
  </React.StrictMode>
);
